#include <iostream>
using namespace std;

int main()
{
    unsigned long long N;
    cin >> N;

    int count = 0;

    while(N > 0)
    {
        N = N & (N - 1);
        count++;
    }

    cout << count;

    return 0;
}
